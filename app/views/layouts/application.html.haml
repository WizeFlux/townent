!!! 5
%head
  %meta{ :charset => "utf-8" }
  %title= content_for?(:title) ? yield(:title) : "Untitled, yet"
  %meta{name: 'description', content: (content_for?(:description) ? yield(:description) : "Undescripted, sorry")}
  %meta{name: 'keywords', content: (content_for?(:keywords) ? yield(:keywords) : "Unkeyworded, sorry")}
  = csrf_meta_tag
  = stylesheet_link_tag :application
  = javascript_include_tag :application
  = javascript_include_tag "https://maps.googleapis.com/maps/api/js?key=AIzaSyDNIzwBp4cCrFqwk-mZ6OIRSMuvP_Uc22U&sensor=true"
  
%body
  #layout
    = render 'shared/header'
    = yield
    #layout-footer
  = render 'shared/footer'