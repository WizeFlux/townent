- events.each do |event|
  %hr
  - unless event.sw_date.to_date == previous_event_date
    - if event.sw_date.to_date == Date.today
      %h3 Today
    - else
      - if event.sw_date.to_date == (Date.today + 1.day)
        %h3 Tomorrow
      - else
        %h3= event.sw_date.strftime('%-d %B %Y')
    

  - previous_event_date = event.sw_date.to_date
  = render 'events/units/event', event: event

- if events.last_page?
  %hr
  #last-page.notificator
    %h4
      There are
      %span no
      more events